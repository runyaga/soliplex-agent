name: soliplex_agent_workspace
description: Pure Dart agent orchestration for Soliplex AI runtime.
publish_to: none

environment:
  sdk: ^3.6.0

workspace:
  - packages/soliplex_agent
  - packages/soliplex_client
  - packages/soliplex_client_native
  - packages/soliplex_logging
